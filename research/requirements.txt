# SENTINEL-WM  -  offline research prototype
# Python 3.10+  (tested on 3.10.11, Windows 11)
#
# Core
numpy>=1.23,<2.0
pandas>=1.5
pyarrow>=10.0
scikit-learn>=1.0
scipy>=1.9

# Deep learning  (CUDA optional - CPU works, ~1 min/40 epochs on the single-day set)
torch>=2.1

# Notebooks + plots
jupyter>=1.0
nbconvert>=7.0
matplotlib>=3.6

# Model-zoo baselines - OPTIONAL. The classical zoo skips these gracefully if
# absent; installing them adds XGBoost + LightGBM to the benchmark.
xgboost>=2.0
lightgbm>=4.0

# Explainability - OPTIONAL. If absent, explain.py falls back to a
# finite-difference / gradient x input attribution with the same output shape.
shap>=0.44

# The GAT baseline is implemented from scratch - NO torch-geometric needed.

# PCAP feature extraction (extraction/extractor.py) - only needed to (re)build
# features from raw .pcap files; not required to run on the provided CSVs.
#   also requires the `tshark` binary on PATH (install Wireshark)
# scapy>=2.5
